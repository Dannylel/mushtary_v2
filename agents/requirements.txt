# ── Core ───────────────────────────────────────────────────────────────────────
pydantic==2.13.4
python-dotenv==1.2.2

# ── LLM: local-first via LangChain/LangGraph (OpenAI-compatible, e.g. Ollama) ───
langgraph==1.2.4
langchain-core==1.4.2
langchain-openai==1.2.2
openai==2.41.0

# ── Tender PDF rendering (main.py / run.py) ────────────────────────────────────
fpdf2==2.8.7

# ── Document ingestion (SOW extractor) ─────────────────────────────────────────
PyMuPDF==1.27.2.3

# ── RAG: local embeddings + flat vector store (agents/rag) ──────────────────────
# Embeddings reuse the same local OpenAI-compatible endpoint (Ollama) as generation;
# only the model differs (default: nomic-embed-text). numpy backs the cosine index.
numpy==2.4.6
tqdm==4.68.2

# ── Demo web UI (FastAPI layer behind the frontend/ console) ───────────────────
fastapi==0.136.3
uvicorn==0.49.0
python-multipart==0.0.32

# ── Async task queue (future production layer; optional for local CLI use) ─────
celery[redis]==5.6.3
redis==6.4.0
